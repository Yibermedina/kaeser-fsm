'use client';
import { usePathname, useRouter, useSearchParams } from 'next/navigation';
import { useTransition } from 'react';
export default function SelectorAnioSabana({ anio, sucursal, anios, sucursales }: { anio: number; sucursal: string; anios: number[]; sucursales: string[] }) { const router = useRouter(); const pathname = usePathname(); const searchParams = useSearchParams(); const [pendiente, iniciar] = useTransition(); function actualizar(clave: string, valor: string) { const params = new URLSearchParams(searchParams.toString()); if (valor) params.set(clave, valor); else params.delete(clave); iniciar(() => router.replace(`${pathname}?${params}`, { scroll: false })); } return <div className="flex items-center gap-2"><select value={anio} onChange={(e) => actualizar('anio', e.target.value)} aria-label="Año" className="rounded-xl border border-[#E4E7EB] bg-white px-3 py-2 text-sm font-semibold">{anios.map((a) => <option key={a} value={a}>{a}</option>)}</select><select value={sucursal} onChange={(e) => actualizar('sucursal', e.target.value)} aria-label="Sucursal" className="rounded-xl border border-[#E4E7EB] bg-white px-3 py-2 text-sm font-semibold"><option value="">Todas las sucursales</option>{sucursales.map((s) => <option key={s} value={s}>{s}</option>)}</select>{pendiente && <span aria-label="Cargando" className="h-4 w-4 animate-spin rounded-full border-2 border-[#E9ECEF] border-t-[#FDC100]" />}</div>; }
